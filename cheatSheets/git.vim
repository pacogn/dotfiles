" Stash{{{
git stash list --data=local                   ==> list stash with date.
" }}}
git difftool {file} = diff file on default difftool
git difftool -t=webstorm [branch --] {file} ==> diff using webstorm 
git reset HEAD^=undo latest commit (and unstage the changes)
