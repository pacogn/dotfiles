⌘ d                     => split vertical
⇧ ⌘ d                   => split horizontal
⌘ [                     => prev split
⌘ ]                     => next split
⇧ ⌘ ⏎                   => zoom split ( full screen and out )
⌘ ⏎                     => toggle iterm full screen
⇥                       => expand selection ( when searching with ⌘ f )
⇧ ⇥                     => expand selection to the left ( when searching with ⌘ f )
⌘ ;                     => autocomplete
⇧ ⌘ H                   => paste history
⌘ ⌥ e                   => tabs expose'
