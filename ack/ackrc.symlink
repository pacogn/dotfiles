
# General ack Settings
##########################################################

# only search with case-sensitivity if there is mixed case
--smart-case
# follow symlinks
--follow

# ignore directories
--ignore-dir=node_modules
--ignore-dir=bower_components

# ignore files
--ignore-file=match:\.rt\.js$
--ignore-file=match:\.rt\.js\.html$

# file types
# --type-set:test:match:/(\.unit.js)|()
